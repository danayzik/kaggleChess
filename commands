Compression command:
tar -czf submission.tar.gz -C kaggle_submissions .
#Run inside kaggle_submissions

Compile command:
g++ -std=c++17 -O2 -o chessBot main.cpp game_tables.cpp evaluation.cpp search.cpp -ffunction-sections -fdata-sections -flto
strip chessBot
#Run in VM